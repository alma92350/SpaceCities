{
  "name": "spacecities",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Multiplayer real-time strategy game in the Stellar Frontier universe, with an MCP interface for AI agent players. No dependencies, no build step.",
  "scripts": {
    "start": "node tools/serve.js",
    "test": "node --test test/*.test.js",
    "test:slow": "node --test test/slow/*.test.js",
    "test:all": "node --test test/*.test.js test/slow/*.test.js",
    "typecheck": "tsc -p jsconfig.json",
    "smoke": "node tools/smoke.js"
  },
  "engines": {
    "node": ">=20"
  }
}
