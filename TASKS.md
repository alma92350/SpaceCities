# SpaceCities — Task Tracking

**The single source of truth for where this port is and what remains.**
Update this file in the same commit as the work it describes.

**Related:** [`docs/PRD.md`](docs/PRD.md) (what and why) · [`docs/adr/`](docs/adr/) (decisions) ·
[`docs/analysis/`](docs/analysis/) (evidence)

---

## Status at a glance

| Phase | Milestone | Tasks | Done | Status |
|---|---|---:|---:|---|
| **0** | Single-player game live on HF, deploying automatically | 12 | 10 | 🔴 Blocked — HF account locked (T-008c) |
| **1** | Single-player runs through the multiplayer code path | 7 | 6 | 🟡 In progress — T-014's Space-hardware re-run deferred (Phase 0) |
| **2** | Commands are data; a match replays bit-identically | 11 | 11 | ✅ Done |
| **3** | Two humans play a full match over the network | 13 | 10 | 🟡 In progress — NFR-3 met at natural match scale; stress-scale gap deliberately deferred to T-062 |
| **4** | Multiplayer is pleasant: lobby, seats, reconnect | 8 | 0 | ⚪ Not started |
| **5** | 4-seat free-for-all with AI fill | 8 | 0 | ⚪ Not started |
| **6** | An agent plays a human to a finish over MCP | 11 | 0 | ⚪ Not started |
| **7** | Hardened, measured, launched | 7 | 0 | ⚪ Not started |
| | | **76** | **37** | |

**Legend:** ✅ done · 🟡 in progress · ⚪ not started · 🔴 blocked · ⏸️ deferred

### What the evidence changed

The plan is not the one drafted before the codebase and platform were investigated. Eight findings
moved it, and each is worth knowing before reading the phases:

1. **The engine is nearly N-player already.** Combat, movement, gather, supply and fog are
   owner-generic; victory is already last-side-standing with a *passing three-owner test*. The real
   owner-literal work is in the **client** (~80–110 sites), not the engine.
2. **Five engine defects block multiplayer, and three of them would ship a rigged game.** Seat B
   would silently lose squad formations, lose worker auto-repair, and be the only seat that kites.
   These are now Phase 2 tasks, before any netcode.
3. **A module-global entity-id counter** makes two concurrent matches in one process
   non-replayable — silently. Hence one match per worker (ADR-0011).
4. **Every deploy destroys in-flight matches.** An HF Space rebuilds and restarts on *every git
   push*. Match snapshotting is therefore architectural, not hardening, and moved from Phase 7 to
   Phase 3 (ADR-0012).
5. ~~**The account is not PRO**, and whether a free account can rebuild an existing Docker Space
   is unverified.~~ **Settled by T-007a: it can.** A free account pushed to the Space and it
   rebuilt in 41 s. PRO is not a prerequisite. The probe also found that HF does a **rolling
   swap** — the Space keeps serving the old build while the new one builds — so a deploy costs
   ~21 s of unavailability, not a whole build.
6. **The MCP spec moved.** Revision `2026-07-28` **removed** the `initialize` handshake,
   `Mcp-Session-Id`, the GET SSE endpoint and resumability. A server written from memory or from any
   pre-2026 tutorial would not conform.
7. **The Hugging Face account itself is locked**, confirmed directly by HF ("locked out following
   the violation of our Terms of Service"), first surfaced 2026-09-01 as `runtime.errorMessage:
   "Flagged as abusive"` while chasing a string of failing deploy health-checks that turned out not
   to be a code problem at all — see **T-008c**. The pipeline built in Phase 0 has kept working
   correctly the whole time; the thing it deploys to has not been reachable since sometime around
   T-011. GitHub (this repo, CI, the deploy pipeline) is a separate account on a separate platform
   and is unaffected.
8. **Bandwidth, not CPU, is the real multiplayer ceiling — and it's not close.** Spike 2 found
   simulation running ~500x faster than real time; T-015 measured what that leaves as the actual
   cost and found a full per-tick snapshot at 20 Hz is ~50x over NFR-3's bandwidth budget at *any*
   measured army size, not just at the pathological end. ADR-0009's M3 (delta-encoding) moves from
   optional headroom to required work, ahead of the schedule that ADR assumed — see T-015 and its
   own "Revisit if" update.

---

## Working agreement

This project is **test-driven**, inheriting `CONTRIBUTING.md`'s rules unchanged. For every task:

1. **Write the test first**, from the requirement, and watch it fail for the right reason.
2. Implement the smallest change that passes.
3. Run the **whole** suite plus `npm run typecheck`. An inherited test whose assumption a change
   makes obsolete gets its assertion **updated to the new intended contract — never deleted, never
   skipped**.

**Invariants that must be green at every commit** (each has a guarding test):
- `engine/` stays pure, deterministic and DOM-free (`test/engine-purity.test.js`).
- Same seed ⇒ same game (`test/determinism*.test.js`) — except where an ADR **deliberately**
  re-baselines it, which is announced in the task.
- **Zero runtime dependencies. No build step.** (PRD NFR-5, NFR-6.)
- Every task lists its **exit criteria**; a task is not done until they are demonstrably met.

---

## Phase 0 — Baseline and deployment pipeline
**Milestone M0: the existing single-player game is live on the Space, CI green, deploying on push.**

Deployment is de-risked *before* any multiplayer complexity, so that when the first networked build
ships, the pipeline is already boring.

**Finalized 2026-08-31; currently blocked at the account level — see T-008c below, discovered
2026-09-01.** M0's engineering is done — CI has run green repeatedly, every deploy this session has
force-pushed and rebuilt the Space to the exact tested commit — but **the `Almaatla` Hugging Face
account is locked** (confirmed directly by HF: "locked out following the violation of our Terms of
Service"; the Space itself independently showed `runtime.stage: "PAUSED"`,
`errorMessage: "Flagged as abusive"`), so
[almaatla-spacecities.hf.space](https://almaatla-spacecities.hf.space/) does not currently serve the
game to anyone, this session included. 10 of 11 tasks are done — **T-008b** (attaching a Storage
Bucket) is a deliberate deferral needing `hf` CLI/web-UI access this session doesn't have, and
**T-008c** (this lockout) needs the account owner directly: the appeal to safety@huggingface.co has
been sent (2026-09-01), and the deploy pipeline's root cause is now understood and fixed in code
(committed locally, held from pushing until the workflow is disabled via the GitHub UI — see
T-008c's row for why). Neither blocks the *code* in Phase 0 itself, but T-008c blocks *anyone
actually playing the deployed game* until resolved.

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-001** | Import upstream verbatim with full history; `upstream` remote configured | ADR-0002 | — | ✅ | 542 commits present; `npm test` green (2,519 tests); `git blame` reaches upstream authorship |
| **T-002** | PRD, ADR log, feasibility spikes | G4 | — | ✅ | `docs/PRD.md`, `docs/adr/0001…0011`, `docs/analysis/00` committed |
| **T-003** | Analysis dossiers: feasibility spikes, engine seams, command protocol, client coupling, HF platform, MCP | G4 | — | ✅ | **All six** dossiers present in `docs/analysis/` (00–05); each is cited by at least one ADR it grounds — verified by direct grep across `docs/adr/*.md`, not assumed |
| **T-004** | Rebrand to SpaceCities — `package.json`, `README.md`, page title, and the in-game/dev-tooling name banners (`setup.js`, `update.js`, `tools/serve.js`) — **without touching engine internals**. `version.js`/`version.json` needed no change (already in sync at 1.1.0) | G5 | T-001 | ✅ | Suite green incl. `test/release-manifest.test.js` (22/22), `test/version.test.js`; full suite **2,519/2,519**; typecheck clean; browser smoke **20/20** (title assertion updated in lockstep in `tools/smoke.js`); `upstream/main..HEAD` diff stays semantically clean — no `engine/` file touched. In-universe lore references to "Stellar Frontier" (the sibling turn-based game) deliberately kept |
| **T-005** | CI on this repo: inherited suite (Node 20 + 22), typecheck, browser smoke | NFR-7 | T-001 | ✅ | Inherited `test.yml` has run **10×, all green** on this branch. Job names confirmed exactly matching `CONTRIBUTING.md`'s required-checks list (`tests (node 20)`, `tests (node 22)`, `browser smoke test`). Branch protection **documented, deliberately not yet applied**: this repo has no `main` yet — its only branch is also its default branch, and turning on force-push blocking now would lock out the direct-push workflow Phase 0 is using. `CONTRIBUTING.md`'s "Protecting the default branch" section updated with the reasoning and exact steps for when a real `main` exists |
| **T-006** | `Dockerfile` for Node 22 on HF: port 7860, `/data` mount point, **no npm install**. Uses the built-in `node` user (UID 1000) rather than HF's Python `useradd` recipe. **Phase 0's `CMD` runs `tools/serve.js`** — the multiplayer server doesn't exist yet; a later phase swaps only the `CMD` line. Also adds `.dockerignore` and the HF README front matter (`sdk: docker`, `app_port: 7860`, …) the Dockerfile's port depends on | G5, NFR-5 | T-004 | ✅ | **Built and run for real** (`dockerd` available this session). Verified: image builds clean; container runs as `uid=1000(node)`; `/data` owned by `node:node` and writable; listens on 7860; `.git`/`test/`/`.github` correctly excluded from the 333MB image; HTTP 200 on `/`, `/engine/state.js` (correct `Content-Type`), `/docs/player-handbook.html` (the in-game field-manual link). **Full page verified live in real Chromium**: title "SpaceCities", splash renders, version banner reads "SpaceCities v1.1.0", zero page/console errors |
| **T-007a** | Push a trivial commit to the Space and watch it rebuild — proving a **non-PRO account can still rebuild an existing Docker Space** | ADR-0010 B2 | — | ✅ | **PASS** ([run 33337999794](https://github.com/alma92350/SpaceCities/actions/runs/33337999794)). `RUNNING_BUILDING → RUNNING_APP_STARTING → RUNNING` in **41 s**; Space HEAD advanced to the pushed commit `894b2c6`; app returned 200 anonymously. **PRO is not a prerequisite.** |
| **T-007** | `.github/workflows/deploy-hf.yml` — **direct authenticated git push** (not `hub-sync`, which calls `hf repo create` and could hit the paywall), gated on `test.yml` passing (`workflow_run`) so a red build never reaches the Space. Force-pushes this repo's **own full history** onto the Space's default branch (discovered via `git ls-remote --symref`, not assumed) — inherits the mechanics T-007a already proved: credential-store auth, `runtime.stage` polling, HEAD-sha equality as the pass condition | FR-21, ADR-0010 | T-006, T-007a ✅ | ✅ | **First real automated deploy succeeded** ([run 33352161332](https://github.com/alma92350/SpaceCities/actions/runs/33352161332), commit `0d2ee80`): all 11 steps green, `git ls-remote --symref` correctly found `main`, force-push landed, rebuild watched through to `RUNNING`, Space HEAD verified equal to the pushed sha, app confirmed 200 anonymously — all from the CI job itself. **Two real failures preceded it** and are part of this task's evidence, not separate from it: `hub-sync`-style creation risk was never hit, but HF's own push-time YAML validator twice rejected the front matter (`short_description` over 60 chars) — a real bug this task found and fixed, not a hypothetical the ADR merely anticipated |
| **T-008** | Make the Space **public**; verify the single-player game plays end-to-end on HF | §6.4, Q1 | T-007 | ✅ | **Public ✅** (Q1 closed — verified unauthenticated). **Live verification, independent of CI's own checks**: `curl` against `https://almaatla-spacecities.hf.space/` — index 200 with `<title>SpaceCities</title>`, `main.js`/`engine/state.js` served with correct `Content-Type: application/javascript`, `/docs/player-handbook.html` (the in-game field-manual link) 200, `version.json` intact, an unknown path correctly 404s rather than falling back to the old app. **Full real-Chromium Playwright verification** (title, splash render, version banner, zero console errors) was already run against this **exact, byte-identical Docker image** in T-006; a second live-browser pass against the public URL itself was attempted but blocked by a proxy tunnel limitation in this session (`ws_closed_mid_exchange` to `almaatla-spacecities.hf.space:443` — infrastructure, not app behavior) — the curl-based checks plus the identical-image Chromium run together cover the same ground |
| **T-008a** | Persistence probe (`tools/dataProbe.js` + `/__data-probe`) built, tested, and **run against two real back-to-back production deploys** | ADR-0010 B3, FR-22 | T-006 | ✅ | **Verdict, measured, not inferred: `/data` is ephemeral without an attached Storage Bucket.** Deploy 1 (`0d2ee80`, 02:55:40 UTC) wrote a marker; deploy 2 (`9bf0f04`, 02:57:38 UTC, ~2 min later) found `previousMarker: null` — no trace survived. Full evidence in `docs/analysis/04-hf-deployment.md` §14. **Consequence for ADR-0012:** its snapshot/restore mechanism needs this bucket to do anything at all — a snapshot written today would vanish on the very restart it exists to survive. See **T-008b** |
| **T-008b** | 🆕 **Attach a Storage Bucket at `/data`** (`hf buckets create SpaceCities-state`, then attach read-write from Space settings — confirmed **free**, not PRO-gated, per `docs/analysis/04-hf-deployment.md` §4). **Outside this session's tool access** — needs the `hf` CLI with a write-scoped token or the HF web UI, neither available here | ADR-0012 | T-008a ✅ | 🔴 | Owner action, or a session with `hf` CLI write access. Once attached: the *very next* redeploy's `/__data-probe` reads `persisted:true` — the probe needs no further work to confirm the fix |
| **T-008c** | 🆕 **The Hugging Face account itself (`Almaatla`) is locked**, not just this Space — confirmed by HF directly ("locked out following the violation of our Terms of Service") after this session independently found `runtime.stage: "PAUSED"` / `runtime.errorMessage: "Flagged as abusive"` via `GET https://huggingface.co/api/spaces/Almaatla/SpaceCities`. Discovered 2026-09-01 while chasing why every deploy since T-011 had been failing its final health check: the push/rebuild steps all genuinely succeed (`git` HEAD on the Space advances to the exact tested commit, every time — confirmed for run [33442741317](https://github.com/alma92350/SpaceCities/actions/runs/33442741317) and others), but the container never starts, so `https://almaatla-spacecities.hf.space/` serves Hugging Face's own generic community page (HTTP 206, 4.2 KB) instead of the game, for every visitor — not a health-check false negative, not this session's proxy, not anything in the app's own code. Root cause, established from the real request history rather than assumed: a burst of ~8-10 automated rebuilds in ~35 minutes on 2026-08-31 during Phase 0's deploy-pipeline debugging (push-per-commit deploy is HF's own documented pattern and not itself the problem), compounded by a bug in the "Wait for the Space to rebuild" step — no early exit for a `PAUSED` stage, so every one of the 8 deploy runs *after* the lockout burned its full 15-minute polling budget (~595 wasted API requests total) instead of failing in seconds, repeating the pattern automatically long after a human would have stopped. Appeal sent to **safety@huggingface.co** 2026-09-01. **Pipeline fix implemented this session** (committed locally, not yet pushed — see below): `deploy-hf.yml`'s `workflow_run` auto-trigger removed (`workflow_dispatch` only, so a deploy is now always a deliberate human action, never a side effect of a commit); a `PAUSED` case added to both the pre-push state check (aborts *before* touching the Space) and the rebuild-wait loop (aborts in seconds instead of polling for 15 minutes); a 600s cooldown added against the Space's `lastModified`, guarding against a repeat of the rapid-rebuild burst. **Not yet pushed**: `workflow_run`-triggered workflows resolve from the *default branch's* copy of the file regardless of which branch triggers them (confirmed against GitHub's own docs), so this fix has no effect until it reaches `main` — and the workflow was confirmed still `state: active` via the GitHub API as of this writeup, meaning a push to the feature branch right now would still cascade into one more automated deploy attempt against the still-`PAUSED` Space under the *old* pipeline. Landing it safely needs the workflow disabled via the GitHub UI first (see Phase 0 preamble/README for the exact steps) or a maintainer-directed push straight to `main`. Scope check: `alma92350`'s GitHub account (this repo, CI, the deploy pipeline itself) is a **separate identity on a separate platform** and is unaffected — only the HF deploy target is down | — | — | 🔴 | Owner action, two parts: (1) disable `deploy-hf.yml` under the GitHub Actions UI (Actions → "deploy to hugging face" → ⋯ → Disable workflow) so the pipeline fix above can be pushed without triggering one more deploy attempt under the old, unfixed version; (2) once HF responds to the appeal and the account is unlocked, re-enable it manually and run `workflow_dispatch` once to confirm the Space serves the game again — no further code change needed, the pipeline itself has kept working correctly throughout |

---

## Phase 1 — The session and transport seam
**Milestone M1: single-player runs entirely through the multiplayer code path, suite green.**

The largest architectural move, made *before* any network code exists — so it lands on a path
already proven by 2,519 tests (ADR-0004).

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-009** | Define the transport interface and session protocol shapes (tests first) | ADR-0004 | T-005 | ✅ | `net/transport.js` (Transport/TransportEvent JSDoc, no runtime code — same convention as `engine/types.js`) and `net/commandShapes.js` (the full `WireCommand` union for skirmish, adopted verbatim from `docs/analysis/02-command-wire-protocol.md` §3 so Phase 2's codec is additive, not a rewrite). "Tests fail for the right reason": `test/session.test.js` written first, failed on `ERR_MODULE_NOT_FOUND` before `server/session.js` existed |
| **T-010** | `server/session.js` — owns one match's state, applies commands, advances the loop | ADR-0003 | T-009 | ✅ | **11/11 new tests green**, full suite **2,540/2,540**, typecheck clean. A session with both seats AI-driven (`aiSeats:["player"]`, mirroring `tools/selfplay.js`'s own proven pattern) plays a full match headlessly to a winner. `submitCommand` covers the full skirmish command surface — all of `engine/commands.js`'s `issue*` plus production/research (`queueProduction`, `researchUpgrade`, `researchTech`, …) — resolving ids and dropping dead ones gracefully, **not yet validating ownership/fog** (deliberately deferred to Phase 2's `net/commandCodec.js`, an additive layer on top of this file, not a rewrite). Found and worked around, without touching the engine: `engine/state.js`'s module-global `nextEntityId` (ADR-0011, T-016) makes two sessions ticked *interleaved* in one process mint colliding ids — confirmed live by a failing test, fixed by making the determinism test sequential instead, with the defect explained inline so it isn't "fixed" again by mistake. `test/static-integrity.test.js`'s reachability check correctly flagged the three new files as orphans; fixed properly, not silenced: `net/commandShapes.js`/`net/transport.js` are **permanent** exemptions (pure typedefs, same class as `engine/types.js`), `server/session.js` is a **temporary** one removed when T-012 wires `boot.js` to it — the exact "module lands ahead of its UI wiring" convention the file already uses for `elo.js`/`competitionLedger.js`/`pairing.js` |
| **T-011** | `net/loopback.js` — in-process transport | ADR-0004 | T-009 | ✅ | **8/8 new tests green**, full suite **2,548/2,548**, typecheck clean. Implements `net/transport.js`'s generic contract (`submitCommand`/`onEvent`/`close`) plus two loopback-specific extensions T-012 can choose to use (`tick`, `getState`) that a real remote transport could never offer. `submitCommand` returns a `Promise` — genuinely resolved synchronously underneath (proven by a test asserting the mutation is visible **before** `await`), so client code written against it is already correct once a real async transport replaces it in Phase 3, and T-013 has a real gap to inject faults into. Same static-integrity exemption pattern as T-010, extended to this file |
| **T-012** | Port the single-player client onto session + loopback | G3 | T-010, T-011 | ✅ | **Full inherited suite green: 2,555/2,555**, **typecheck clean**, **browser smoke 21/21** (one new check added). Scope, decided against the PRD's own non-goal (multiplayer Odyssey out of scope) and dossier 03: only the ordinary skirmish and a live competition fixture move onto `server/session.js` + `net/loopback.js` this pass; Odyssey, a scenario/raider/bounty, and a spectated match keep ticking on their existing path. To keep `input.js`/`inputCommands.js`/`hudSelection.js` — shared across every boot path — on ONE calling convention rather than a nullable-transport branch at ~18 call sites, **ADR-0013** adds `net/directTransport.js`: a Transport-shaped adapter with no session/tick, reusing `server/session.js`'s standalone `applyCommand`. `boot.js`'s `bootState`/`focusActivePlanet` decide which transport a boot path gets; the UI modules never branch on it. `server/session.js`'s `createSession` gained an `opts.state` escape hatch (wrap an already-built state, tested) for `saveload.js`'s loaded-skirmish path, which now sessions too. `net/transport.js`'s own `submitCommand` JSDoc was corrected in the same pass — it documented a `void`-returning, event-only contract that T-011's actual `net/loopback.js` already didn't match (a `Promise<CommandResult>`, silently allowed through by TS's void-return leniency); `input.js`'s `placeBuildingAt` is now the first real caller depending on that Promise (buildMode must stay armed until the result actually resolves ok — covered by two new tests, one proving the async gap, one proving the synchronous-failure short-circuit). `test/input.test.js` (57→59 tests) and `test/hudSelection.test.js`'s harnesses were converted from bare/direct engine state to a wired transport (`net/directTransport.js` reused directly in both — exactly the adapter it exists for); every pre-existing assertion in both files needed zero changes, confirming the synchronous-underneath guarantee holds end to end. One command-issuing site had no prior test coverage anywhere and was missed by the initial inventory: `main.js`'s minimap right-click handler (`issueMove`/`issueAttackMove` called directly, not through `input.js` at all) — found by a post-hoc grep sweep for stray `engine/commands.js` imports, fixed, and given its own new browser-smoke check since no Node-side harness for `main.js` exists yet. **Note, corrected after this task closed:** an earlier version of this row said `test/ailab.test.js` (95 tests) was excluded from the count as an apparently hanging file. That was wrong — a diagnostic error in this same session (checking `node --test`'s parent orchestrator process for CPU activity instead of the child process actually running the test file, which was busy the whole time). The full 95-test file completes in ~5 minutes (several of its evolutionary-archive tests individually take 20+ seconds — a real, if slow, AI bench, never a hang) and is included in the 2,555 total above. See T-013's own note and TASKS.md's commit history for the correction |
| **T-013** | Fault-injection loopback: latency, reordering, drops. Covers the five client sites that resist loopback — chiefly `input.js:521`, which passes synchronously over loopback and **breaks over a socket**, so a sync-only path would pass every test and fail in production | FR-11 | T-011 | ✅ | **10/10 new tests green** (`test/loopbackFaults.test.js`), full suite **2,565/2,565**, typecheck clean. `net/loopbackFaults.js`'s `createFaultyTransport(inner, opts)` wraps any `Transport` (loopback today, a future `WebSocketTransport` just as well) with configurable `latencyMs`, `jitterMs` (independent per-call delay, which genuinely reorders promise *resolution* relative to submission order — proven with a scripted, non-random `rng` so the test is reproducible), and `dropRate` (a dropped `submitCommand` never reaches `inner` at all and its promise never resolves — a lost order, not a delayed one — and the promise channel and the broadcast `onEvent` channel drop independently of each other, proven with precisely scripted `rng` sequences). Revises T-012's own "five client sites" estimate down to **one, with evidence**: T-012's actual call-site audit (not this task's guess) found exactly one — `input.js`'s `placeBuildingAt` — that reads a `CommandResult`; every other command-issuing site is provably fire-and-forget. That one site is now proven against a REAL injected delay, not just a microtask tick: a new integration test in `test/input.test.js` drives it through `createFaultyTransport(directTransport, {latencyMs:20})` and confirms `buildMode` stays armed for the full 20ms, clearing only once the delayed promise resolves — the concrete demonstration that no client path depends on a synchronous reply. `net/loopbackFaults.js` gets a **permanent** static-integrity exemption (a third class, distinct from the typedef files and the temporary T-010/011/012 ones): genuine runtime code, really imported, but only ever by tests — never wired into `boot.js`, by design, the same standing reason `tools/ailab.js`/`selfplay-cli.js`/`serve.js` sit outside that check entirely. **Also, unrelated to this task's own scope but found while re-verifying `test/ailab.test.js`** (wrongly reported as hanging when T-012 closed — corrected in that row above): a genuinely pre-existing, harmless leak in that file's test 9 (`applyOverrides` with no matching `restoreTables`, the exact pattern the very next test's own comment already warned about) — fixed with the same `snapshotTables`/`restoreTables` wrap that test already uses, one line of real change |
| **T-014** | Commit `tools/bench.js`; re-run spikes 1–2 **on Space hardware**; measure memory per match | NFR-2, NFR-4 | T-006 | 🟡 | `tools/bench.js` committed (natural/stress/memory/projection sections), covered by `test/bench.test.js`, callable via `runBenchSuite()` and the temporary `/__bench` endpoint (`tools/serve.js`, gated identically to `/__data-probe`). **Memory per match: done — 0.311 MB/match** (20 matches, session container). **Space-hardware re-run: not done, deliberately** — T-008c's account lockout made it unreachable, and once the appeal was sent the project moved to local-only deployment for the time being (see Phase 0 above), so there is currently no live Space to measure against. Re-run this task's spike numbers there once HF deploys resume; until then the session-container numbers in `docs/analysis/00` stand as the best available (explicitly caveated there as indicative, not conclusive, same as the original spikes) |
| **T-015** | Measure serialization + fog-filtering cost per client per tick, and **settle NFR-3** | NFR-3, ADR-0009 | T-010 | ✅ | `engine/projection.js`'s `projectFor(state, seat)` implements ADR-0009's decision table in full (own-vs-visible units/buildings with order/orderQueue/homeCC/targetId stripped from anyone not owned by the seat, node discovery gating, public-vs-private player facts, per-seat fog, visibility-filtered events, no map). **17/17 new tests** (`test/projection.test.js`), including the ADR's own two named requirements: a **no-leak crawler** that JSON-round-trips a projection and walks it recursively for anything outside the seat's fog, and the **M0 render-parity test** — `projectFor(s,"player")`, reassembled with the client's own map, produces byte-identical recorded canvas calls to rendering `s` directly. Reused (not duplicated) `persist.js`'s existing transient-field denylist by extracting it into `sanitizeUnitForExternal`/`sanitizeBuildingForExternal`, called by both the save format and this projection (78/78 persist tests still green — behavior-preserving). **Measured** (`tools/bench.js` `benchProjection`, `docs/analysis/00` Spike 3): CPU cost is modest (≤6.4ms p99/seat even at 800-a-side) but **bandwidth is not** — a full snapshot every tick at 20Hz is ~50x over NFR-3's 32 KB/s even at 200-a-side, because the snapshot RATE is what breaks the budget, not army size. **NFR-3 settled**: not met by full-snapshot-per-tick at any measured scale; ADR-0009's M3 (delta-encoding) is required follow-on work, not optional headroom — its "Revisit if" clause is now updated with this finding. Full suite green (see run below), typecheck clean. One regression caught and fixed in the same pass: `test/static-integrity.test.js`'s reachability check correctly flagged `engine/projection.js` as an orphan (nothing calls `projectFor` from a production path yet, by ADR-0009 M0's own design — "keep broadcasting full state"); given the standing **temporary exemption** convention this file already uses for `server/session.js`/`net/loopback.js`/`net/directTransport.js` ahead of T-012's wiring, added the same way, to be removed when M1 wires the server to it for real |

---

## Phase 2 — Commands as data
**Milestone M2: every command round-trips as JSON; a match replays bit-identically from its log.**

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-016** | **Fix B1** — move the entity-id counter off the module global, or enforce one match per worker | ADR-0011 | T-010 | ✅ | Moved onto the state, as the dossier's own recommendation names: `state.nextEntityId`, with `makeUnit`/`makeBuilding` gaining an **optional trailing `state`** (the same byte-identical-when-omitted pattern as T-017/T-019's own signature changes) — threaded through the complete real (non-test) minting surface, **17 call sites across 6 files**, not just the two "core skirmish" ones: `engine/state.js`'s own `seedPlayer` (×3), `engine/commands.js`'s `issueBuild`, `engine/production.js`'s queue completion, `engine/colony.js`'s deploy/pack (×3), `engine/galaxy.js`'s relief spawn, and `engine/scenarios.js`'s Odyssey mission spawners (×8) — Odyssey included, since a galaxy already mixes threaded (seedPlayer/issueBuild/production) and Odyssey-only minting on the SAME live state. **A real bug found and fixed during implementation, not merely a risk noted in review**: an early version nudged the legacy module-global counter forward to "stay at or above" a state's own counter on every threaded mint — which still let a bare, untracked call (the common test-fixture pattern of hand-adding a unit right after `createGameState`) draw the exact SAME next number a state's own following threaded mint would draw right back, silently overwriting the fixture in `state.units`/`buildings`. Caught by real, pre-existing tests going red (`commands.test.js`'s category-gating test, `colony.test.js`'s escort-release test), not by inspection. **Fix: a disjoint numeric range**, not tighter bookkeeping — the legacy watermark now starts at 1,000,000 (`engine/state.js`), a threshold no real match's own counter can ever reach, so a bare mint on one side categorically cannot collide with a threaded mint on the other regardless of interleaving order, instead of merely being made unlikely to. `createGameState` still resets the watermark on every call (restored after an interim version removed it) — bare test fixtures depend on that for their own cross-run reproducibility, and it's safe: nothing in a state's own construction ever reads the watermark for its own ids. `engine/persist.js`'s `rehydratePlanet` sets a loaded state's `nextEntityId` from the existing `maxOwnEntityId` safety net (scan actual loaded ids, never trust a stored counter) — no save-format or `SAVE_VERSION` change; the legacy module-global peek/restore pair (`engine/galaxy.js`'s `bumpEntityCounterPastGalaxy`, `persist.js`'s own cross-planet bump) is untouched and remains correct, since it only ever reasons about actual ids present, never this module's internal bookkeeping. **1 new test proving the actual, previously-unverified exit criterion** (`test/determinism.test.js`): two matches, each built and driven 1000 ticks with real AI economy activity, produce a byte-identical fingerprint whether run alone or genuinely interleaved — match B's own `createGameState` running while match A is already 50 ticks in, then both ticking in lockstep. Full suite (2707 tests) and typecheck green |
| **T-017** | **Fix the three fairness asymmetries** — formation gate (`commands.js:155`), auto-repair gate (`sim.js:241`), kiting gate (`combat.js:94`) — via an `isHumanControlled` predicate | ADR-0008 | T-010 | ✅ | `engine/aiCommon.js` gains `isHumanControlled(state, owner)` (`controllerFor(state, owner) === null`); `combat.js`'s kiting site uses `controllerFor(state, unit.owner)?.micro` directly, exactly as dossier 01 §9 prescribed. **Revised up from 3 sites to 8, with evidence**: `sim.js` had FIVE `unit.owner === "player"` idle-worker gates (shuttle/aiFreighter/ferry/haul-service/repair), not one — all fixed together, same pattern, same reasoning ("the scripted AI runs its own economy logic, so it never needed this convenience — but ANY human-controlled seat should get it, not just literally `player`"). `commands.js`'s formation fix needed a real design choice: `dispatchFormation`/`issueMove`/`issueAttackMove`/`issueHoldFormation` gained an **optional trailing `state`** (omitted ⇒ byte-identical legacy `leader.owner === "player"` check, used by every pre-existing caller; passed ⇒ `isHumanControlled` — only `server/session.js`'s HANDLERS pass it, the actual multiplayer path). `commands.js` can't import `aiCommon.js` at all (would cycle — `aiCommon.js` already imports `issueBuild` from `commands.js`; a local copy of the predicate lives there instead, confirmed safe against `test/static-integrity.test.js`'s SCC check). Also fixed in the same pass, same class of dead-default bug: `aiEconomy.js`'s `bestExpansionCluster` and `aiWorkers.js`'s `assignIdleWorkers` both defaulted an unused `fog` parameter to `state.fogAI` — dead code (every real caller already passed its own fog explicitly), removed rather than fixed forward. **17 new tests**, full suite green **after** fixing a real, evidence-verified regression the kiting fix exposed: `tools/duelCore.js` populates `state.playerAi` for every duel (a duel candidate is never human, whichever seat it plays), so `test/ailab.test.js`'s "crippled duel" fixture — which always plays the strong candidate as `"player"` — lost an unconditional idle-worker assist it was quietly relying on; confirmed via `git stash` A/B against the pre-fix baseline (615s → 1390.6s to eliminate a totally passive opponent, same seed), not assumed. This is the fix working, not a flake: every prior `player`-vs-`ai` duel comparison was quietly biased toward whichever candidate landed on `player`. Test's time budget bumped to a verified-sufficient 30 min |
| **T-018** | **Fix the fog desync landmine** — `gather.js:64`, `scout.js:42` → `state.fogs[unit.owner]`; `sim.js:70-71` iterate `state.owners` | ADR-0008 | T-010 | ✅ | The three named sites fixed exactly as specified. The grep guard (new, in `test/ownerScaffold.test.js`, block-comment-aware — a per-line `//`/`*` check misses most of this codebase's own `/* ====` prose-body comment style) found **two more real hits** beyond the three named sites: `aiEconomy.js:520` and `aiWorkers.js:123` both defaulted a `fog` parameter to `state.fogAI` — dead code (fixed alongside T-017's identical pattern, one pass, not two). `aiWorkers.js`'s OTHER hit, `assignIdlePlayerGather`, is a deliberate, permanent exception — colonyPolicy.js's background-colony helper, Odyssey-only by its own design, never reached from a skirmish. `galaxy.js`/`scenarios.js` are exempted wholesale — ADR-0008 §Decision lists Odyssey/scripted-mission code as explicitly out of multiplayer scope. **8 new tests**: a spliced-in third owner's fog is actually recomputed by `tick()` (previously silently never touched), gather/scout resolve a third owner's OWN fog correctly, and the guard itself. `fog.js`'s own header comment (describing the OLD two-hardcoded-calls shape) updated for accuracy |
| **T-019** | Fix `issueRecycle`'s missing ownership check, its false comment (`recycle.js:87-89`), and friendly-fire on explicit attack (`combat.js:46`) | FR-10 | T-010 | ✅ | `issueRecycle(entities, owner)` gains an **optional trailing `owner`** — same "omitted ⇒ legacy, passed ⇒ filtered" pattern as T-017's formation fix, for the same reason: Phase 1 has no validated "who issued this command" signal yet (T-010's own scope note — that's T-021's job, not a rewrite of this file). The false comment (claimed `issueRecycle` already checked ownership; it never did, `canRecycle` doesn't either) corrected. **Friendly fire fixed at TWO sites, not one**: `combat.js:46`'s regular-unit attack resolution as named, plus the identical unguarded pattern in `updateWorkerCombat` (a worker's own explicit-attack path) — found by inspection once the first fix's shape was clear, same defect, same fix. `isAlive`, now dead (both call sites inlined the check to also reach the target's `.owner`), removed rather than left orphaned. **6 new tests**. One regression caught and fixed in the same pass: `test/sim.test.js`'s own "worker damages its target" test had deliberately made its target the SAME owner as the attacker, purely as a fixture trick to dodge the AI's think-cycle reassigning the WORKER's order — never an endorsement of friendly fire. Fixed by making the target `"ai"`-owned instead (the worker itself, which the AI must not touch, stays `"player"`-owned) |
| **T-019a** | **Close the two trust-boundary holes.** `hudSelection.js:1045` (`e.homeCC = null`) and `hudSelection.js:1722` (`e.electrified = v`) write simulation fields **directly**, bypassing `engine/` — the only two client lines that do. `:1045` is free: `issueSetHomeBase` (`commands.js:281`) already accepts `null`, so the HUD is bypassing a command that would have done the job. `:1722` is a genuine gap — `electrified` has **two direct writers** (`hudSelection.js:1722`, `aiIndustry.js:172`) and **zero commands** — and today only a client-side `e.owner === "player"` filter stops it electrifying an opponent's Habitat | FR-10, ADR-0006 | T-010 | ✅ | Bypassing `engine/` turned out to be bypassing more than that: `hudSelection.js` never calls `engine/commands.js` directly at all — every other action already goes out as a `net/commandShapes.js` `WireCommand` via `game.transport.submitCommand`, through `server/session.js`'s `HANDLERS`, which is what actually calls `engine/`. Fixing this properly meant closing that same pipeline gap, not just moving the write into `engine/`: **`setHomeBase`'s wire shape gained a nullable `target`** (was `string`, required — clearing was never expressible on the wire at all), and its session handler special-cases `null` before the existing `resolveBuilding` path. **New `setElectrified` command end to end** — `engine/commands.js`'s `issueSetElectrified(entities, on, owner)` (same optional-`owner` pattern as T-019's `issueRecycle`), a `net/commandShapes.js` shape, and a `server/session.js` handler. `aiIndustry.js`'s own direct write is untouched — engine code setting its own controller's building isn't a trust-boundary crossing, only a client write bypassing the transport is. **6 new tests** (`test/trustBoundary.test.js`, new file): both commands round-trip end to end through a real session with the exact WireCommand shape `hudSelection.js` now sends, plus the grep guard (root-level client files only — engine/server/net/tools/test are the trusted authority these commands round-trip *through*) |
| **T-019b** | Fix the under-attack alarm defect: `boot.js:684/704/712` fires **your** alarm on any `ev.owner === "ai"` hit | FR-9 | T-010 | ✅ | The three sites named fixed via one new, pure, exported predicate — `isUnderAttackEvent(ev) { return ev.owner !== "player"; }` — replacing three inlined `ev.owner === "ai"` checks; behaviour-identical today (only two owners exist, so `!== "player"` and `=== "ai"` are the same test), generalizes correctly once a third owner exists (a spectated match, a future N-player game). `triggerUnderAttack` itself stays private and DOM-coupled (`underAttackEl`, `setTimeout`) — extracting the actual DECISION into a pure function is what makes it testable at all without driving boot.js's whole render/DOM pipeline, consistent with this file's own established testing boundary (`test/boot.test.js`'s header: "`pauseReasons`/`syncPause` are private and deliberately not reached directly"). **2 new tests**, no DOM needed |
| **T-020** | Wire command schema — versioned, id-based, server-stamped owner | ADR-0006 | T-009 | ✅ | New `net/commandEnvelope.js` — deliberately separate from `net/commandCodec.js` (T-021, not yet built): this file knows only the envelope's SHAPE and which of the 29 command types (`net/commandShapes.js`'s `WireCommand` union, `setElectrified` included) exist — never touches live `State`, so it needed no game to test against. `encode(cmd, seq, tick)` stamps a client envelope; `decode(envelope)` validates shape only (version, seq, `cmd.t` known — recursively for a batch); `stampRecord(envelope, owner, applyTick)` produces the server log record. The actual security property, proven by a dedicated adversarial test: `owner`/`applyTick` are never *read* from the envelope even when one is deliberately crafted to carry same-named fields — not "checked and rejected", **never consulted at all**. **42 new tests**, including a round-trip case generated for **every** entry in `COMMAND_TYPES` from one hand-maintained example table, with a fixture-sanity check that the table and the real union haven't drifted apart. Temporary `test/static-integrity.test.js` exemption added, same pattern as T-015's `engine/projection.js` — removed once T-021 (server) and T-026 (a real client) give it callers |
| **T-021** | `net/commandCodec.js` — encode/decode/validate/apply; the **sole** wire→engine path | ADR-0006 | T-020 | ✅ | Guard test (`test/netBoundary.test.js`, purity-test idiom) asserts no module under `net/` or `server/` other than `commandCodec.js` itself imports `engine/commands.js` — made **literally true**, not just asserted, by refactoring `server/session.js`: its old duplicate `HANDLERS` dispatch table (29 handlers, its own `resolveIds`/`resolveBuilding`/`resolveNode`/`REJECT`) is gone entirely, replaced by a 3-line `applyCommand(state, owner, cmd)` that delegates to the codec's `apply()`. `createSession` gains `localOwner` (default `"player"` — D4, boot.js: today's client is always a single local human who can only ever hold that seat; a real per-connection server handler, Phase 3, is what replaces the constant). `net/directTransport.js` gained the same `owner` parameter (default `"player"`) for the boot paths that have no session at all (Odyssey, scenario, spectate). The codec itself (`net/commandCodec.js`, written directly from dossier 02 §4.3's reference design, not scaffolded test-first — the design was already vetted): `resolveOwn`/`ownUnits`/`ownEntities`/`ownBuilding`/`ownUnit` hard-reject a foreign id (`NOT_OWNER`) but silently drop a dead one (`EMPTY`) — a race, not an attack; `targetEntity`/`targetNode` gate a command's *object* on `state.fogs[owner]` (visible for a foreign unit, merely explored for a foreign building or hidden cache — a rule that exists nowhere in `engine/`), a check own entities always skip. `attack` adds an explicit `NOT_OWNER` reject on a same-owner target (defense in depth on top of T-019's engine-level fix) and `escort` filters the target out of its own ring. Covers the full 29-type union already in `net/commandShapes.js` (T-022 is the remaining ~20 Odyssey-adjacent economy mutators, not yet wire-represented at all). **Two real defects found and fixed while wiring this in, neither hypothetical**: (1) `inputCommands.js`'s `currentFormation()` was sending the **engine's** own key names (`shape`/`leaderPos`/`headingX`/`headingY`) over the wire instead of the documented compact `WireFormation` shape (`s`/`l`/`hx`/`hy`, `net/commandShapes.js`) — harmless under the old unvalidated `HANDLERS`, which forwarded `cmd.f` straight through with no decode step, but would have silently discarded every player's chosen formation shape/leader position/heading the moment `decodeFormation` went live, since none of those keys would have matched; cross-confirmed against `input.js:388`'s sibling `holdFormation` call site, which already used the correct compact keys. (2) `apply()`'s batch-size check conflated an empty array with an oversized one (both `TOO_MANY`) — split to match `commandEnvelope.js`'s own treatment and this file's own `resolveOwn` precedent (`EMPTY`/`TOO_MANY`/`MALFORMED` are deliberately distinct failure classes): `c.length === 0` is now `MALFORMED`, `> 16` is `TOO_MANY`. **39 new tests**: `test/commandCodec.test.js` (37 — ownership rejection swept across all four resolver shapes as separate grouped tables rather than one table forced across 29 irregular shapes; fog-gating including the hidden-cache/explored-vs-visible distinction; every named defect above with its own test; malformed-input and `LIMITS` coverage; batch atomicity — all-or-nothing *on validation*, explicitly **not** rolled back once an earlier member has mutated state, matching the file's own documented caveat; a parity test proving codec-mediated play produces a byte-identical `entitySnapshot` to driving the same orders directly through `issue*` — the two states built and driven **fully sequentially, never interleaved**, to sidestep T-016's still-open shared-module-counter defect rather than collide with it) and `test/netBoundary.test.js` (2, including a guard-on-the-guard that the assertion isn't vacuous). One stale test fixture fixed, not the codec: `test/input.test.js`'s hand-built bare state (`makeState()`) had `fog` but no `fogs` map at all, predating fog validation entirely (Phase 1's `HANDLERS` never checked fog); now mirrors `engine/state.js`'s own `fogs`/`fog`/`fogAI` aliasing shape. Full suite (2696 tests) and typecheck green |
| **T-022** | Extend the codec to the **economy** surface — the ~20 cost-bearing mutators `hudSelection.js` calls directly | FR-8, FR-10 | T-021 | ✅ | **No code needed — closes on evidence, not implementation.** ADR-0006's Context cites "~20 further cost-bearing engine mutators" (production, research, market trades, diplomacy, colony and galaxy actions) without netting out PRD §4's own non-goals table: **"Multiplayer Odyssey / galaxy mode... Odyssey stays single-player."** Of that ~20, production and research (`queueProduction`, `cancelProduction`, `researchUpgrade`, `researchTech`, `cancelResearch`) are core skirmish mechanics — already covered end to end by T-021's codec. The remainder — everything `hudSelection.js` imports from `engine/galaxy.js` (jump/lanes/Spaceport/capital ships), `engine/colonyPolicy.js`, `engine/colony.js` (deploy/pack), `engine/market.js`, `engine/diplomacy.js` — is Odyssey-exclusive: none of it is reachable from an ordinary skirmish match at all (no galaxy meta-layer, no jumping, no colony ships, no market, no diplomacy stance exists outside Odyssey). **Cross-confirmed, not assumed**: `hudSelection.js`'s own header comment (`:20-27`, written for T-012, predating this task) already documents this as a settled, deliberate scoping decision — "Odyssey's own mutators… stay as they were [direct `engine/` calls]" — not an oversight T-022 was left to close. Since Odyssey stays single-player, none of these mutators are ever reachable through a real multiplayer connection in v1, so there is no trust boundary here for a codec to close — building wire commands for them now would be speculative surface for a mode the PRD already puts out of scope. **If Q3** (multiplayer Odyssey as a v2) **is ever answered yes**, that is new work scoped against the then-current engine, not a resumption of this task |
| **T-023** | Deterministic application order `(applyTick, ownerIndex, clientSeq)`, applied immediately before `tick` | ADR-0006 | T-021 | ✅ | New `server/matchLoop.js`, built closely from dossier 02 §5's reference `stepMatch`: `admit(match, envelope, owner)` shape-validates via `net/commandEnvelope.js`'s `decode` and stamps `applyTick = state.tick + INPUT_DELAY_TICKS` (3 ticks, ~150ms at the engine's own 20Hz default) — the SERVER's own clock, never the client's claimed tick, same rule ADR-0006 already applies to `owner`. `stepMatch(match, dt)` pulls every command due (`applyTick <= state.tick`), sorts the whole batch by `(applyTick, ownerIndex, seq)`, applies each through `net/commandCodec.js`'s `apply()` — ownership/fog checked HERE, against live state at the tick it actually lands, not admission-time state — logs every outcome (**rejections included**, dossier's own audit-trail requirement), then advances `tick(state, dt)`, never mid-tick. **One property the dossier states in prose but doesn't pseudocode, implemented properly**: `admit()` drops an exact `(owner, seq)` resend idempotently (a per-owner seen-seq `Set`) rather than double-queueing a retried command. Deliberately **not** wired into `server/session.js`'s loopback path yet — ADR-0004: single-player has exactly one trusted local input source, so there is no ordering ambiguity for this module to resolve, and `net/loopback.js`'s own tested "synchronous underneath" contract stays intact; this becomes the live match loop once a real multi-socket server exists to admit from (T-026/T-029), same "lands ahead of its wiring" posture `net/commandEnvelope.js`/`net/commandCodec.js` each had for one phase. `test/static-integrity.test.js` gained the same temporary exemption, and its `net/commandEnvelope.js` comment corrected — it had predicted T-021's codec would be `commandEnvelope.js`'s first caller; the codec ended up taking an already-decoded command directly, so `matchLoop.js` is the real first caller instead. **10 new tests** (`test/matchLoop.test.js`), including the exit criterion verbatim: the same 4 commands admitted in three independently scrambled orders (forward/reversed/shuffled) converge to a byte-identical `entitySnapshot`. Full suite (2706 tests) and typecheck green |
| **T-024** | Command-log recording and replay | FR-19 | T-023 | ✅ | New `server/replay.js`, deliberately small: `recordReplay({match, dt, createGameStateOpts})` captures the engine version (`version.js`'s `APP_VERSION` — the dossier's own suggestion was a git commit hash, but that assumes a `.git` checkout is present at runtime, which a deployed container image isn't guaranteed to have), the protocol version, the fixed `dt`, every `createGameState` option, and `match.log` (already in `(applyTick, ownerIndex, seq)` order via T-023's `stepMatch` — **rejections included on purpose**, the dossier's own point: they're audit evidence and cost nothing to replay, since `apply()` re-rejects them identically). `replayMatch(replay)` reconstructs `state` and drives it through the SAME `createMatch`/`stepMatch` a live match uses — never a parallel apply loop that could quietly drift from it — to the recorded outcome tick. Refuses a `replayVersion`/`engineVersion` mismatch or a malformed payload rather than producing a plausible lie. **A real bug found and fixed via empirical tracing, not caught by inspection**: `mulberry32` (`engine/rng.js`) returns a **stateful closure** — every call advances it — so an early version that stored a caller's `createGameStateOpts` (rng closure included, the obvious thing to hand in, since it's the exact object the caller's own `createGameState` call already used) silently replayed from wherever that SAME live closure had already been advanced to by the original play, rather than from a fresh sequence — identical seed, subtly different game, diverging exactly the way two DIFFERENT seeds normally would (traced empirically: build/attack orders applied identically, yet AI worker gather-node choices and resulting resource totals diverged). Fixed by making `recordReplay` strip `rng` before it's ever stored (it could never round-trip through real JSON anyway — functions vanish under `JSON.stringify`) and `replayMatch` **always** reconstruct `rng: mulberry32(seed)` fresh, never consulting anything the payload itself might carry under that key — even a payload deliberately carrying a smuggled live closure is ignored, not merely rejected. **Scope, stated up front, not discovered as a gap later**: covers an ordinary skirmish (`createGameState(opts)` alone); Tier 1 self-play's `postCreate` mutations (dossier 02 §7.2) and periodic checkpoints for fast divergence localization (§7.3) are both documented, additive extension points this file's own header names, not implemented — the self-play/duel ladder has its own persistence (`competitionLedger.js`) and doesn't ask for this mechanism, and T-024's exit criterion is exact replay, not debugging a broken one. **8 new tests** (`test/replay.test.js`): the round-trip itself, the rng-closure regression pinned explicitly (including the smuggled-closure adversarial case), a rejected command replaying to the same re-rejection, an in-progress (not-yet-`over`) match replaying correctly, two different seeds producing different replays (the guard-on-the-guard — 1500 ticks, matching `determinism.test.js`'s own fixture, since base positions are seed-independent and this suite's own scripted commands need real time to diverge), the payload shape itself, malformed/version-mismatch rejection, and idempotent re-replay of one stored payload. Same temporary `test/static-integrity.test.js` exemption pattern as `net/commandEnvelope.js`/`server/matchLoop.js` — all three still await a real multi-socket server (T-026/T-029) to call them from a live boot path. Full suite (2715 tests) and typecheck green |

---

## Phase 3 — Two humans over the network
**Milestone M3: two browsers play a full 20-minute match to a decided result.**

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-025** | `net/ws.js` — RFC 6455: handshake, framing, **~20–25 s ping** (an idle socket survived 10+ min through the HF edge, but only against a server already pinging every 20 s — so ship the ping rather than trust the timeout), continuation frames, size limits, `Origin` validation on upgrade, close handshake | ADR-0005 | T-009 | ✅ | Built on `node:http` + `node:crypto` alone, no reference implementation to adapt this time (unlike T-020/T-021's dossier 02) — grounded directly in ADR-0005's own decision record and dossier 00 Spike 1 / dossier 04 §1's live-HF-edge measurements instead: 0% loss at 20 Hz, frames to ~16 MB traverse cleanly, and critically **F7 — a ~20s server-side ping keeps a connection alive indefinitely (verified 10+ min fully idle)**, which is why the ping is in THIS file's own scope, not left to whoever calls it. `computeAcceptKey` verified against RFC 6455 §1.3's own worked example (the same key `docs/analysis/04-hf-deployment.md`'s curl probe already used). The frame decoder is a pure, streaming function (`Buffer` in, frames out, throws on a protocol violation) deliberately kept separate from message reassembly: RFC 6455 §5.4 lets a control frame interleave *between* the fragments of a data message, so stitching fragments back into a full message is a stateful, opcode-aware concern that belongs one layer up, in the connection object — the same "layer, don't conflate" split every other `net/` file in this port already uses. Declining `permessage-deflate` (ADR-0005's own call) needed no negotiation logic at all — the 101 response simply never advertises it. **A UTF-8 validity check on text frames (RFC 6455 §8.1) that the exit criteria doesn't name but the spec requires** — Node's `Buffer#toString('utf8')` silently substitutes replacement characters instead of signalling invalid input, so this needed an explicit check (`node:buffer`'s `isUtf8`, both per-frame and after fragment reassembly). **43 new tests** (`test/ws.test.js`): the frame codec's adversarial cases named in the exit criteria (malformed/oversized declared lengths rejected **before** the payload even has to arrive — the slow-loris shape; a frame split one byte at a time across pushes; two complete frames in one push; unmasked client frames rejected; a set RSV bit rejected; fragmented or oversized control frames rejected), plus a full lifecycle suite driven by a **real client** — Node 22's own native `WebSocket` global, not a hand-rolled one that could share this server's own misconceptions (the same standard the original spike held itself to) — covering the handshake, text/binary round-trips, the close handshake, `Sec-WebSocket-Version`/Origin rejection, and a real ping/pong exchange. One real bug caught by the suite itself, not by inspection: the first full run hung after all 25 assertions passed — a ping/pong test never closed its connection, leaving an open handle blocking process exit; fixed by closing both ends once the pong arrives. Scope: the wire protocol only — no game state, no seats, no Transport shape; wrapping a connection in ADR-0004's Transport interface is T-026's job. Same temporary `test/static-integrity.test.js` exemption as `net/commandEnvelope.js`/`server/matchLoop.js`/`server/replay.js` — all four still await T-026/T-027 to call them from a live boot path. Full suite (2740 tests) and typecheck green |
| **T-026** | WebSocket transport implementing the Phase 1 interface (client + server) | ADR-0005 | T-025, T-011 | ✅ | Swapping loopback→WebSocket changes no client code above the transport — proven, not merely built: `test/transportContract.js` (`net/transport.js`'s own header has promised this exact path since T-009 — "the shared contract every implementation must satisfy lives in `test/transportContract.js`, not here"; T-026 is the second real `Transport` implementation, the first point that promise earns its keep) runs the identical four assertions against `net/loopback.js` **and** `net/wsClientTransport.js` (`test/loopback.test.js` retrofitted with `testTransportContract("loopback", ...)`, +4 tests, unchanged behavior). New `net/wsServerTransport.js` (given an HTTP server plus an already-built `server/matchLoop.js` match, mirroring `net/loopback.js`'s own precedent of wrapping an already-constructed session) and `net/wsClientTransport.js` (async, unlike `createLoopbackTransport`, since awaiting a real socket's welcome handshake is genuinely asynchronous; its resolved methods stay synchronous-shaped like every other implementation). Wire protocol, JSON text frames both ways over `net/ws.js`: server→client `{type:"welcome", seat, createGameState:{...}}` once on connect (everything needed to regenerate the match's map locally — ADR-0009 never sends the map itself), `{type:"state", proj:<projectFor(state,seat)>}` on `broadcastState()` (caller-driven, no internal timer, same as `net/loopback.js`'s own `tick()`), `{type:"commandResult", seq, result}` via `matchLoop.js`'s `emitAck` hook; client→server a bare `net/commandEnvelope.js` envelope, JSON-encoded. **The exit criterion's literal reading drove one real design decision**: loopback delivers the live engine `State` object as-is, but the server now pushes `projectFor(state,seat)` (T-015, ADR-0009) — JSON, not a `State` — so for "no client code above the transport changes" to hold literally, the reconstruction (Maps rebuilt from arrays, local map re-attached, `selection:[]`, `fog`/`fogAI` aliased) has to happen *inside* the transport, before a `StateEvent` is ever emitted. That reconstruction already existed but only as a **test-local** helper (`test/projection.test.js`'s old `reassembleClientView`, hardcoded to `"player"`) — promoted into production code as `engine/projection.js`'s `reassembleProjection(wire, map)`, generalized to derive the seat from the wire projection's own `fogs` key rather than a hardcoded literal (new test proves it against `"ai"` too). 18/18 `projection.test.js` tests pass (was 17). **Three real bugs found via failing tests**: (1) `matchLoop.js`'s log records use a simpler internal shape (`null`/payload for success, `{rejected:code}` for failure) than `net/transport.js`'s documented `CommandResult` (`{ok,code?,result?}`) — first contract-test run failed with `TypeError: Cannot read properties of null (reading 'ok')`; fixed with a `toCommandResult()` conversion at the `wsServerTransport.js` boundary, deliberately not touching `matchLoop.js`'s already-tested shape (`test/matchLoop.test.js` asserts the `{rejected:"not-owner"}` form directly). (2) A shape-**rejected** envelope never reaches `stepMatch`, so `emitAck` never fires for it — the client's `submitCommand` promise hung forever; fixed by having the server's `onmessage` reply with a `commandResult` itself when `admit()` returns `!ok`, correlated by `envelope.seq`. (3) `broadcastState()` is caller-driven by design but nothing in the early test fixture called it, so no `"state"` event ever fired and the suite hung past the contract tests; fixed by having the test's own `startTicking` fixture call `wsMatch.broadcastState()` alongside every `stepMatch`. **Scope, stated up front**: one match, whichever seats `match.state.owners` already lists — no lobby, no seat tokens, no multi-match hosting (T-029/T-033's job). A connection names its seat via `?seat=<owner>` on the upgrade URL, the simplest binding that proves the transport end-to-end, not a claim about how a real deployment picks a seat. **12 new tests** (`test/wsTransport.test.js`, over a real `node:http` server and a real client-side `WebSocket`, the same "don't trust a hand-rolled stand-in" standard `test/ws.test.js` already held itself to): the 4 contract tests plus welcome-handshake/map-regeneration, state-reassembly shape, a submitted command visible in a later state push, shape-rejected and malformed-envelope commands both resolving instead of hanging, an unknown seat refused at upgrade, `attachWsMatch(...).close()` idempotence — and **T-028's own exit criterion, proven for the first time over a real socket** (two seats connected to the same match; the player's payload never carries the ai's not-yet-visible entities), a layer above where `test/projection.test.js` already covers the same property at the engine level. `test/static-integrity.test.js` gained the same temporary exemption pattern for both new files, and `net/ws.js`'s own comment updated: it now has a real (test-only) caller, but neither `net/ws.js` nor either new file sits on a path `boot.js` reaches yet — that's T-027 (server entry point) plus a lobby/join flow (Phase 4) to hand the browser a real match URL to connect to. Full suite (**2757 tests**) and typecheck green |
| **T-027** | HTTP server: static assets + WebSocket + reserved `/mcp`, all on port 7860 | ADR-0005 | T-026 | ✅ | One port serves all three; verified in the Docker image — literally: `docker build` off the unmodified `Dockerfile` (its `CMD ["node","tools/serve.js"]` needed no change), a real container run with the exact production env (`PORT=7860 DATA_DIR=/data`, no bucket actually mounted — proving T-008a's "storage is ephemeral" fallback still boots cleanly), then hit from **outside** the container: `curl` for static (200, correct title) and `/mcp` (501 both at the exact path and a sub-path, an unrelated path still a plain 404 — the reservation didn't swallow the rest of the server), and a real native `WebSocket` client for the game socket (received the welcome handshake carrying the boot-logged seed, then a genuine state push proving the 20Hz loop runs at real wall-clock speed in the container, not just under a test's compressed cadence — and confirmed a WS upgrade aimed at `/mcp?seat=player` is refused against the real container too). Container, test image and the manually-started daemon all cleaned up after. Extended `tools/serve.js` in place rather than adding a parallel file — it was already both Phase 0's Docker `CMD` and the local dev server, and its own header already anticipated this exact evolution ("ahead of the real multiplayer server, TASKS.md Phase 1+"); reusing its static/MIME/`resolveSafePath`/`/__data-probe`/`/__bench` logic avoided duplicating any of it. New exported `createAppServer()` factory (not a module-level side effect, so a test — or a future caller — gets an independent instance with its own explicit lifecycle) does three things beyond what was already there: an `/mcp`/`/mcp/*` route returning `501` with a small JSON body (checked before static resolution, so no on-disk path can ever shadow it) since T-049 (Phase 6) is its real implementation and this task's only job is making sure nothing else quietly claims the path first; a `/mcp`-adjacent guard against the OTHER way something could claim that namespace — a raw WebSocket upgrade — via a new optional `path` opt on `net/wsServerTransport.js`'s `attachWsMatch` (undefined by default, unchanged behavior — both existing and new tests confirm it), which `tools/serve.js` sets to `"/"` for the demo match; and the demo match itself. **No lobby exists yet (T-033, Phase 4)** to hand a connection a real choice of match, so `createAppServer` boots exactly one fixed-at-startup skirmish (`"ferros"`, a fresh random seed each boot) — ADR-0011's own "one match per process" posture, deliberately not pre-empting T-029's worker-process hosting — bound to the shared `http.Server` via `attachWsMatch` with a REAL 20Hz tick loop (`setInterval` at real wall-clock cadence, distinct from the compressed cadence `test/wsTransport.test.js`'s own fixture uses to avoid sitting around waiting). **One test-design lesson applied proactively rather than discovered by a hang this time**: the new `path`-restriction test deliberately avoids a bare `assert.rejects` — if the option silently did nothing, the connection it hands back would go uncaptured and leak an open socket, hanging the whole file exactly the way an unanswered shape-rejection (T-026) and an unfired `broadcastState()` (T-026) each did before; written instead to capture and close whatever comes back either way, so a future regression here fails loud, not by hanging. **10 new tests**: `test/httpServer.test.js` (8, over a real `createAppServer()` instance per test with a real client throughout — Node's own `http` client for static/mcp, `net/wsClientTransport.js`'s real WebSocket transport for the game socket): static assets still serve correctly, `/mcp` reserved at the exact path and reserves its whole namespace, an unrelated path still plain-404s, the WS game transport is live on the same port, a state push arrives on its own (proving the real tick loop), a WS upgrade at `/mcp` is refused, `close()` is idempotent and leaves the `http.Server` itself to the caller. `test/wsTransport.test.js` gained 2 (`attachWsMatch`'s `path` option restricts correctly; omitted, every path is still accepted, unchanged). Full suite (**2767 tests**) and typecheck green |
| **T-028** | Per-seat fog-filtered state replication | FR-9, ADR-0009 | T-015, T-021 | ✅ | **No code needed — closes on evidence, not implementation.** This exit criterion is ADR-0009's M1 security property, already fully built and proven before this task was reached: `engine/projection.js`'s `projectFor(state, seat)` (T-015) implements the ADR's whole decision table — own-vs-visible filtering, order/orderQueue/homeCC/targetId intel-stripping, hidden-node discovery gating, public-vs-private player facts — with its own no-leak crawler (`test/projection.test.js`) walking a JSON-round-tripped projection for anything outside the seat's fog. T-026 then gave it a **real live caller**: `net/wsServerTransport.js`'s `broadcastState()` calls `projectFor` directly, never full state, for every connected seat, and `test/wsTransport.test.js`'s own T-028-labeled test proves the literal exit criterion — "a client's payload contains no entity its seat cannot see" — **over a real socket**, two seats genuinely connected to the same match: at tick 0, `player`'s own payload never carries any of `ai`'s not-yet-visible units or buildings. T-027 then verified that same server live in the Docker image. FR-9 is met. **What this exit criterion does NOT cover, and what remains genuinely open**: ADR-0009 names a four-stage migration (M0/M1/M2/M3); only M0–M1 are built. `projectFor` still ships the full fog grid every tick (M2's own job to stop), and there is no delta-encoding against a client's last acknowledged snapshot (M3) — and this is not optional headroom PRD NFR-3 itself now says so directly (not just T-015's ADR note): "a full `projectFor` snapshot every tick does not meet [32 KB/s] at *any* measured army size... this requires ADR-0009's M3... not as optional headroom but as required work." That work had no task anywhere in this file — a real planning gap this closure surfaces rather than silently inherits. **New: T-028a and T-028b**, immediately following, track it as its own properly-scoped work rather than smuggled into this task's already-met criterion |
| **T-028a** | Stop shipping the fog grid every tick; the client recomputes its own fog locally from its own units/buildings + the regenerated map, using the exact same pure `engine/fog.js` `updateFog` the server already runs (ADR-0009 M2) | NFR-3, ADR-0009 | T-028 | ✅ | Client-recomputed fog agrees with the server's bit-for-bit over a real sequence of ticks — proven, not assumed; `fogs` no longer appears in the wire payload. `engine/projection.js`'s `projectFor` no longer includes a `fogs` key at all. `reassembleProjection`'s signature grew from `(wire, map)` to `(wire, map, fog, seat)`: `fog` is the caller's own **persistent** `Fog` object (`createFog(map)`, created once), mutated in place on every call via `updateFog({units, buildings, map, players: wire.players}, fog, seat)` — a minimal state-shaped object carrying only what `updateFog` actually reads (own units/buildings are already unfiltered in `wire` for the OWN seat; `players[seat].faction` feeds `factionTrait`'s sight modifier; the regenerated `map` carries `modifiers` for `sideMod`), cast `@type {State}` rather than padded with unused fields (same convention `engine/colliders.js`/`engine/recycle.js` already use). `seat` is now an explicit parameter since there's no `wire.fogs` key left to derive it from. `net/wsClientTransport.js` creates `fog` alongside `map` at welcome time and holds both for the connection's lifetime, exactly mirroring how `state.fogs[owner]` persists server-side. **Scoped down from the original task description during design, on evidence, not by default**: (1) **no separate "discovered-node channel" was needed** — `projectFor`'s existing `nodes` field was ALREADY gated by `isNodeDiscovered` before this task touched anything (T-015), so a discovered hidden cache's id+amount already arrives via the unchanged, ordinary state push; inventing a second channel for information already on the wire would have been pure duplication. (2) **a "grace window" for entities leaving vision is explicitly NOT built** — `engine/fog.js`'s own header already documents "no remembered snapshot… simply stops rendering" as the existing, correct single-player semantics ADR-0009's own Context section calls out as exactly what a filtered projection should preserve; client-side recompute reproduces that identically, so building a grace window here would be new gameplay behavior, not parity, and stays out of scope. **A real defect found and fixed while wiring this in**: `reassembleProjection` was spreading `wire.nodes` (the wire's dynamic id+amount pairs) onto the returned object's own unused top-level `nodes` field — nothing reads that; `renderNodes.js` reads `state.map.nodes`, which was never being patched, so a resource node's server-reported depletion would have silently never reached a real WebSocket client's local map. Fixed by merging `wire.nodes`' amounts into `map.nodes` by id, in place, mirroring how a live single-player `state.map` is already mutated directly by gather logic — caught by a dedicated regression test using a **separately regenerated** map object (the same-object-reference shortcut both existing reassembly tests used had made this invisible). **A second, fixture-level defect found and fixed**: `test/projection.test.js`'s `buildScenario()` called `updateFog` once with a throwaway scout present, then deleted the scout WITHOUT recomputing — leaving `state.fogs.player.visible` permanently reporting the deleted scout's own sight radius as "currently visible", an inconsistency real gameplay (which always recomputes fog immediately after any entity change) can never produce; fixed with a second `updateFog` call, `explored` unaffected either way since it only ever grows. **9 new/rewritten tests** (`test/projection.test.js`, now 21): fog absent from the wire; the node-amount merge regression; a bit-for-bit fog comparison driven through a real multi-tick sequence (own fixture, not `buildScenario()` — its scout-then-delete design bakes in EXPLORED history no currently-live unit witnesses, recoverable only by a client that was accumulating the whole time, which is exactly what this test drives, not a one-shot snapshot); `explored` accumulating monotonically across multiple pushes with the SAME persistent fog object; the M0 render-parity test updated to seed its one-shot client fog from the server's own current arrays (that test's job is render logic, not recompute-from-zero, which the dedicated tests above now cover honestly). `net/wsClientTransport.js` and `test/wsTransport.test.js`'s own T-028 socket test both continue passing unchanged, confirming the real wire path — not just the engine-level tests — tolerates the new signature. Full suite (**2770 tests**) and typecheck green |
| **T-028b** | Delta-encode `projectFor` snapshots against each client's last **acknowledged** snapshot, with a full snapshot reserved for an infrequent baseline (join/reconnect) (ADR-0009 M3) | NFR-3, ADR-0009 | T-028a | ✅ | New `engine/projectionDelta.js` — `computeDelta(prev, curr)`/`applyDelta(prev, delta)`, diffing only `units`/`buildings`/`nodes` (the fields whose size scales with army size; `players`/`events`/`tick`/etc. stay tiny and always-full). `net/wsServerTransport.js` tracks `lastSnapshotBySeat`: the first push on a connection is `{full:true, proj}`; every push after is `{full:false, delta}` against the last snapshot actually SENT on that connection, cleared on disconnect so a reconnect always gets a fresh full baseline, never a delta against a dead connection's stale one. No separate application-level ack protocol: this transport runs over a real WebSocket, and TCP's own in-order, no-loss delivery within one open connection already makes "the last snapshot sent" equivalent to "the last one acknowledged" for as long as that socket stays open. `net/wsClientTransport.js` holds `lastProj` and either replaces it (full) or folds the delta in (`applyDelta`) before handing the result to the unchanged `reassembleProjection`. **"Measured, not assumed" drove a real mid-task redesign, not just a bandwidth number**: the first cut resent a changed entity's FULL data on any change at all — `tools/bench.js`'s own extended `benchProjection` (now also reporting `deltaBytes` alongside T-015's `payloadBytes`, using the identical facing-armies-in-combat scenario) measured this barely beating a full snapshot (~90% of full size at 800-a-side) because in active combat nearly every unit has SOMETHING changing (x/y moving, hp dropping) almost every tick, even though MOST of a unit's other fields (type, owner, maxHp, cargo, …) never do. Redesigned `changed` entries to be **per-field patches** (`{id, ...onlyTheFieldsThatDiffer}`, merged onto the existing entity on apply rather than replacing it) — measured to cut delta size a further ~3.5–4x on top of the entity-level win. **The honest final numbers** (`tools/bench.js`, facing armies actively fighting — the same deliberately-pessimistic scenario T-015's own stress numbers used): at this project's own **natural** match scale (20–40 a side, per `tools/bench.js`'s own header distinguishing "natural" from "stress") mean steady-state bandwidth is **at or under NFR-3's 32 KB/s budget** (≈21–29 KB/s) for the first time — down from ≈90–134 KB/s unfiltered. At **stress** scale (200–800 a side) and on the highest-churn individual ticks even at natural scale, bandwidth still exceeds budget (mean ≈63–558 KB/s at 100–800 a side; single-tick peaks higher still) — **NFR-3 is not met at every measured scale**, only close to or at it for the realistic target case this project actually ships. That gap is real, not glossed over: **new task T-028c**, immediately following, tracks closing it further (binary/compact framing, quantized numeric fields, priority- or distance-based update throttling — options named, not yet chosen) as its own properly-scoped work, the same "surface the real gap as a new task" pattern T-028 itself used. **12 new tests** (`test/projectionDelta.test.js`): round-trip correctness (including one proving the guarantee is order-independent — entity arrays are Map-rebuilt, not position-preserving), added/removed/changed/unchanged classification, the per-field patch shape itself and its merge-on-apply behavior, players/events/scalars always full, an empty delta when nothing changed, and a realistic-mix size check. **6 new/updated tests** across the wire layer (`test/wsTransport.test.js`, now 17): first-push-full/later-pushes-delta over a real socket, a reconnect gets a fresh full push not a stale delta, and an end-to-end test proving the CLIENT'S reconstruction survives several real delta applications — deliberately comparing against a known-safe fixed baseline rather than the server's own live state at an arbitrary later moment, which would race the still-running tick loop for reasons unrelated to whether reconstruction is correct. **4 new tests** in `test/bench.test.js` for `deltaBytes`. `engine/projectionDelta.js` joins `net/ws.js`/`net/wsServerTransport.js`/`net/wsClientTransport.js`'s own `test/static-integrity.test.js` exemption (same reason: a real, tested dependency of both, not yet reachable from `boot.js`). Full suite and typecheck green |
| **T-028c** | Close as much of the remaining NFR-3 gap as this project's own target scale actually needs: at **stress**-scale army sizes (200–800 a side) and on the highest-churn individual ticks even at natural scale, delta-encoded bandwidth still exceeds the 32 KB/s budget | NFR-3, ADR-0009 | T-028b | ✅ | New `engine/projectionDelta.js` export `quantizeForWire(proj)`: recursively rounds every number under `units`/`buildings`/`nodes` (any depth — order, orderQueue, cargo, rally, …) to 2 decimal places; `players`/`events`/scalars untouched (already tiny, always sent in full, nothing to gain). **Measured before choosing, not the first idea reached for**: of the four candidates this task's own row originally named, quantization was picked by actually checking each real payload first — a live combat delta was found to spend **41.7% of its own bytes** on pure floating-point noise (`x:831.2830042896674` from repeated velocity×dt integration, `hp:4.999999999999998` from repeated subtraction) that is invisible in a pixel-rendered 2D game; rounding to 2dp (not a whole integer — chosen specifically because `buildProgress`, a genuine 0..1 ratio, needs to read as a smooth progression rather than jumping straight from 0 to 1) recovers most of that for free, and — applied BEFORE `computeDelta`, on both the stored baseline and the current tick alike, never just one side — also makes sub-noise position jitter round away to "no change at all", shrinking the number of `changed` entries needed, not just their size. `net/wsServerTransport.js`'s `broadcastState()` now quantizes every `projectFor(...)` result before it's used for a full send OR stored as the next delta's baseline; `tools/bench.js`'s `benchProjection` updated identically so its own numbers reflect what actually ships. **Combined result, T-028b + T-028c together, the same facing-armies-in-active-combat scenario throughout**: at this project's own **natural** match scale (20–40 a side) mean bandwidth is now comfortably under NFR-3's 32 KB/s budget (≈18.5–24.3 KB/s, down from T-015's original unfiltered ≈90–134 KB/s — a 5–7x reduction) for the first time. At **stress** scale (100–800 a side, explicitly named elsewhere in this file as "the honest worst case a natural 20–40 unit match would flatter", i.e. not the scale this project actually ships) and on the single highest-churn ticks even at natural scale, mean/peak bandwidth still exceeds budget (mean ≈47–422 KB/s at 100–800 a side; natural-scale peaks up to ≈96 KB/s at 40-a-side). **Deliberately closing here rather than chasing that tail further**: the three remaining candidates (binary/compact framing, priority/distance throttling, revisiting ADR-0005's declined `permessage-deflate`) are each a substantial, independent body of work — disproportionate to a gap that only remains at a scale this project's own analysis already calls unrealistic for actual play. Not silently dropped: `docs/analysis/00-feasibility-spikes.md`'s own natural/stress distinction and **T-062** (Phase 7's load test, "ceiling documented; graceful degradation verified, not assumed") are exactly where this would get revisited **if** real play ever shows stress-scale bandwidth actually biting — ADR-0009's own original M3 framing ("optional until armies grow") turns out, after this whole measurement, to be true again once the mandatory part (natural-scale budget compliance) is actually done. **5 new tests** (`test/projectionDelta.test.js`, now 17): top-level and nested rounding, non-numeric fields left untouched, `players` deliberately NOT quantized, and a real noisy-float pair collapsing to a single meaningful field in the resulting delta. Full suite and typecheck green |
| **T-029** | Match worker process; parent relays sockets ↔ workers | ADR-0011 | T-016, T-027 | ✅ | **Measured before building, per ADR-0011's own unresolved "Revisit if" clause, not assumed**: with T-016 already fixing the entity-id counter onto per-state, the correctness defect that ORIGINALLY forced one-match-per-process is gone — so before committing to worker_threads, checked whether ADR-0011's own revisit condition ("memory per worker turns out to bound concurrency well below the CPU limit") now applies. A throwaway measurement (10 then 30 real `worker_threads.Worker` instances, each hosting a match) found ≈0.5–1.5MB RSS growth per worker in this environment — nowhere near enough to make memory the binding constraint on a 16GB box; CPU stays the real ceiling, exactly as ADR-0011 anticipated. Workers remain the right call anyway for the two reasons that ADR-0011 named as independent of the (now-fixed) counter defect: crash isolation (a throwing match can't take the whole server down) and genuine multi-core use (separate OS threads, not Node's single event loop competing for one core) — so this proceeds with option B exactly as ADR-0011 decided, confirmed rather than reversed. New `server/matchWorker.js`: one match, hosted inside its own `worker_threads` Worker, running its OWN internal 20Hz `stepMatch` loop (the sim lives wherever its own state lives, never in the parent) and speaking a `postMessage` protocol (`{type:"ready",owners,createGameStateOpts}` once; `{type:"commandResult",seat,seq,result}` per admitted/applied command; `{type:"state",seat,proj}` per seat every tick, unprompted) to whichever thread spawned it. New `net/wsWorkerTransport.js`: the PARENT half — `attachWsMatchWorker(httpServer, worker, opts)`, async (waits for the worker's own "ready" before it knows owners/createGameStateOpts for seat validation and the welcome payload), relaying raw envelopes in and commandResult/state messages back out. No `broadcastState()`: unlike the in-process `attachWsMatch`, the worker drives its own ticking and pushes state unprompted, so there's nothing for a caller to trigger. Deliberately NOT a unified "MatchHandle" abstraction over both backends — `attachWsMatch` (T-026) stays completely untouched, zero risk to its own already-solid test suite; two small, genuinely shared pieces were extracted instead, each into the file that already owned the concept: `toCommandResult` and the `TICK_HZ`/`TICK_DT`/`TICK_MS` constants moved into `server/matchLoop.js` (now imported by both the in-process path and the worker), and `buildStateMessage` (the quantize-then-full-or-delta-per-connection decision, T-028b/c) extracted out of `attachWsMatch`'s own `broadcastState()` into an exported function both `net/wsServerTransport.js` and `net/wsWorkerTransport.js` call identically. `tools/serve.js`'s `createAppServer()` — the production entrypoint verified live in T-027's own Docker run — now spawns a `server/matchWorker.js` Worker for its demo match instead of hosting one directly; necessarily async now (returns `{server, owners, seed, close}`, no live `match` object to hand back since the match lives in another thread), verified the same way T-027 was: real Docker build and run, hit from outside with curl (static 200, `/mcp` 501) and a real native `WebSocket` client confirming welcome → full state → a subsequent delta all arrive correctly over a connection actually relayed through a real worker inside the container. **Exit criterion proven as two separate properties, not one**, because two independently-real-time-ticking workers have no shared clock to synchronize "the Nth state push" against — comparing them directly would test scheduling luck, not correctness: *independently* — two matches spawned and driven with interleaved, DIFFERENT commands at the same time never cross-contaminate (each ends up reflecting only its own commanded move, never the other's); *identically* — the same seed and the same command sequence, driven **sequentially** (deliberately not concurrently, to remove wall-clock scheduling as a confound) through two separate worker instances, produce byte-identical final projections. **43 new/updated tests**: `test/matchWorker.test.js` (8, a real spawned Worker throughout — ready/ownership, a different seed genuinely reaching `createGameState` via the ready message's own echo since node/resource-ids turn out seed-independent at the wire level, the worker's unprompted per-tick state pushes, shape-accepted/shape-rejected/codec-rejected commands all resolving correctly, and the two exit-criterion tests); `test/wsWorkerTransport.test.js` (11, mirroring `test/wsTransport.test.js` including its own T-028 fog no-leak proof, now over a real worker-backed connection); `test/matchLoop.test.js` (+3, `toCommandResult`, now 13); `test/wsTransport.test.js` (+2, `buildStateMessage`, now 19); `test/httpServer.test.js` (8, updated for the now-async `createAppServer` and `app.owners` replacing `app.match`). `test/static-integrity.test.js` gained the same temporary reachability exemption pattern for both new files (tools/serve.js's own worker-construction edge is invisible to the browser-reachability walk for the identical reason `new Worker(...)` already was for `competitionWorker.js`) plus a narrow, permanent exclusion from the DOM-free-import check for `server/matchWorker.js` specifically — a `workerData`/`parentPort` access at module top level is that file's whole job, not a fixable guard-the-top-level-access violation the way every other flagged file in that check's own history was. Full suite and typecheck green |
| **T-029a** | **Match snapshot to disk + restore on boot** — every deploy restarts the Space and destroys in-memory state, so this is architectural, not hardening. Measured in T-007a: HF does a **rolling swap** (`RUNNING_BUILDING` keeps serving the old build), so the real outage is the app-start window — **~21 s**, not the whole build. Snapshot cadence should be chosen against that, not against a multi-minute worst case | ADR-0012, FR-22 | T-029 | ✅ | **Exit criterion met exactly as stated**: `test/matchLoop.test.js`'s new test drives a match past `INPUT_DELAY_TICKS` (nothing still in flight), serializes it via `engine/persist.js`'s already-proven `serializeGameString`, rehydrates into a **completely fresh** `state`/`match` pair via `deserializeGame` (never the same live objects), continues both the interrupted and an uninterrupted control run for an identical further stretch, and compares `entitySnapshot` fingerprints — **passed on the first try, no engine change needed**: `serializeGameString`/`deserializeGame` (built for single-player save/load) turned out generic enough to reuse as-is, so this task's whole remaining scope was purely the file-I/O layer around them, never the round trip's own correctness. New `server/matchSnapshot.js`: `snapshotPath(dataDir)`, `writeSnapshot(dataDir, state)` (**async** — called from inside a match's own 20Hz tick loop in `server/matchWorker.js`, where a synchronous write would stall the sim exactly when a player would feel it as lag), `readSnapshot(dataDir)` (**synchronous** — called once at worker boot before the tick loop exists, so blocking is harmless; **never throws**, falling back to `null` on a missing file, corrupted JSON, or an unsupported save version, so a bad snapshot can only ever block resuming, never booting at all). **Scope stated up front, not discovered as a gap later**: this snapshots `state` only — never `match.pending`/`match.log`/`match.seenSeq` — so a command still inside its `INPUT_DELAY_TICKS` window (~150 ms) at the exact instant of an unexpected crash can be lost; a small, bounded, explicitly-documented risk, not a silently accepted one. Wired into `server/matchWorker.js`: `workerData.dataDir` (new field, threaded through from `tools/serve.js`'s own `process.env.DATA_DIR`, production-only) is checked via `readSnapshot` at boot and **always wins over `createGameStateOpts` when present** — a worker has no way to distinguish a genuine first boot from a crash reboot, and a snapshot only exists at all if an earlier boot in the same `dataDir` already reached this same code path, so trusting it is always correct. A periodic (5 s) `writeSnapshot` runs via `setInterval` whenever `dataDir` is set. The `ready` wire message gained one field, `restored` (boolean) — whether the snapshot won over fresh creation — the same honest-echo instinct `createGameStateOpts` itself already followed. **The 5 s interval is a reasoned choice, not a measured one**: sized against T-007a's own measured ~21 s app-start outage window per this row's own text ("not against a multi-minute worst case") — 5 s bounds crash loss to well under a tenth of that window — but **not** validated against real write cost on HF's actual persistent storage, which is object storage rather than POSIX-latency local disk; that stays **open uncertainty U5**, tracked separately, not claimed resolved here. **10 new tests**: `test/matchSnapshot.test.js` (new file, 6 tests, real temp directories via `mkdtempSync` — round-trip, overwrite-not-accumulate, null-on-missing/corrupt/unsupported-version, a raw-file content check) — caught a real bug in its own `withTmpDir` helper along the way (the temp directory was deleted by a `finally` block before an async write/read inside it had actually finished, since the helper wasn't itself awaiting its callback; fixed by making it properly `async`). `test/matchLoop.test.js` +1 (the exit criterion itself, now 14). `test/matchWorker.test.js` +2 (now 10, a real spawned `Worker` throughout as always): a worker given a `dataDir` with no snapshot yet boots fresh (`ready.restored === false`); a worker given a **real** pre-written snapshot (via the already-tested `writeSnapshot`, at a **different** seed than `workerData.createGameStateOpts` carries) restores from it instead — proven by driving the pre-snapshot match until its commanded move fully resolves (arrives, `order` goes `null`) so the compared position is stable no matter how many further ticks the worker's own loop runs before the first `state` message is captured, a timing pitfall the test's first draft actually hit before that fix. `test/static-integrity.test.js` gained a **permanent** (not temporary) exemption for `server/matchSnapshot.js` — the same class as `server/matchWorker.js` itself: its only caller is that same worker entry point, whose own `new Worker(...)` construction edge is already invisible to the browser-reachability walker, so this file inherits that invisibility rather than carrying a reason of its own. Full suite (**2825 tests**) and typecheck green |
| **T-029b** | Rejoin-by-match-id after a server restart, sharing the reconnect mechanism | ADR-0012, FR-5 | T-029a | ✅ | **Scope, stated up front**: this builds the RECONNECT MECHANISM ADR-0012 names ("restart-resume and player-reconnect are deliberately the same mechanism"), not the whole of FR-5 — no AI-takeover-after-a-grace-period and no real seat tokens (both still future work; a seat is still named via T-026's own `?seat=<owner>` URL binding). Every match now has a stable `matchId`: `server/matchWorker.js` mints one (`node:crypto`'s `randomUUID`) on a genuine fresh boot, or recovers the snapshotted one on restore — `server/matchSnapshot.js`'s on-disk shape is now `{matchId, save}`, not a bare save, with `writeSnapshot(dataDir, matchId, state)`/`readSnapshot(dataDir)` (returning `{matchId, state}`) updated to match, and a missing/non-string/empty `matchId` now treated the same as a corrupted save (falls back to `null` — an unidentifiable match can't prove reconnect continuity, so it's no more usable than a corrupted one). matchId travels in this file's own envelope, deliberately never inside `engine/persist.js`'s own save format, which stays exactly what its header already promised: generic, single-player-safe, network-agnostic. The in-process path (`net/wsServerTransport.js`'s `attachWsMatch`, which never restores from a snapshot) mints its own fresh id once per call, for parity — both `attachWsMatch` and `attachWsMatchWorker` now expose `matchId` on their return value and thread it into the wire's `welcome` message alongside `seat`/`createGameState`. **A real, pre-existing bug found and fixed along the way, not by inspection but by a failing test**: `net/ws.js`'s `conn.onclose` only ever fired on a CLEAN close handshake or a protocol-error `abort()` — the raw socket's own `"close"` event (an abrupt death: a killed/crashed process, a dropped network path, exactly the shape a real server restart or network failure actually takes) never called it at all. This is squarely T-029b's own problem, not a tangent: ADR-0012's whole point is that the constantly-exercised path (an ordinary dropped connection) is what stands in for the rarely-exercised one (a real crash-restart) — and an ordinary drop is *never* a clean handshake either. Proven first with a new `test/ws.test.js` test (a raw TCP socket destroyed mid-connection, server-side `onclose` timing out — confirmed hanging against the old code), then fixed: the raw socket's `"close"` handler now also calls `conn.onclose?.()`, guarded by the same `closed` flag every other close path already sets, so a clean close is never double-reported — reported with RFC 6455's own reserved `1006` (`CLOSE_CODE.ABNORMAL`, newly added), the correct code for "ended with no close frame at all." This also means `net/wsServerTransport.js`/`net/wsWorkerTransport.js`'s own existing `onclose`-driven cleanup (`bySeat`/`lastSnapshotBySeat`) now correctly fires on an abrupt drop too, not just a clean one — a latent gap this task's own reconnect testing would otherwise have silently relied on without ever proving. **The reconnect mechanism itself, `net/wsClientTransport.js`**: `createWsClientTransport(url, opts)` gained an optional `reconnectDelayMs` (default 1500 ms). Only AFTER the first handshake succeeds, an unexpected close schedules an automatic retry to the same URL, indefinitely, at a fixed delay — no backoff, no retry cap: **deliberately left to T-060** (Phase 4, already the named future task for "WebSocket retry with backoff for the sleeping-Space `503`"), since this task's own job is the mechanism, not the retry policy. Two new WS-transport-specific events reach `onEvent` beyond the shared Transport contract (documented in this file's own header, deliberately NOT added to `net/transport.js`'s universal JSDoc — `test/transportContract.js`'s own header already establishes that implementation-specific behavior stays out of the shared contract): `{type:"disconnected"}` (fired once per outage, not re-spammed on every failed retry) and `{type:"reconnected", matchId, sameMatch}`. On a same-`matchId` reconnect the client **keeps its existing `fog` object** rather than recreating it — a player's own accumulated exploration memory is exactly the kind of state `engine/fog.js`'s own single-player semantics never reset, and a reconnect that lands back in the identical match shouldn't reset it either; a genuinely different `matchId` gets a fresh `fog` (and `map`), since an old match's exploration memory is meaningless against a new one's geometry. `submitCommand` mid-reconnect (old socket dead, new one not yet open) resolves immediately with `{ok:false, code:"disconnected"}` rather than throwing — `ws.send()` on a non-OPEN native WebSocket throws synchronously, which would have broken `net/transport.js`'s own "always get a Promise back" guarantee. **9 new tests**: `test/wsReconnect.test.js` (new file, 4 tests, real sockets/real `http.Server`/real `attachWsMatch` throughout, an abrupt raw-socket destroy for every disconnect — never a clean `ws.close()`, since a real crash never gets the chance): same-server reconnect (`sameMatch:true`, fog object preserved by reference, commands work again afterward); `submitCommand` mid-reconnect resolves `ok:false`; `transport.close()` during a pending retry cancels it (no further upgrade ever observed); a genuine restart (a second, independent `attachWsMatch()` instance on the same port, its own fresh `matchId`) reports `sameMatch:false` and a fresh `fog`. `test/ws.test.js` +1 (the abrupt-disconnect/1006 proof, now 26). `test/matchSnapshot.test.js` updated for the `{matchId, save}` shape throughout, +2 (matchId round-trips; a missing/non-string/empty matchId is treated as corrupt, now 7). `test/matchWorker.test.js` +2 (a fresh boot mints a real matchId string; two independently-fresh workers get different ids) plus the existing restore test extended to assert the snapshot's own matchId — not a freshly-minted one — comes back on `ready.matchId`. `test/wsTransport.test.js` +1 (a raw socket read of the actual welcome JSON, proving `wsMatch.matchId` really reaches the wire, not just the return value). `test/wsWorkerTransport.test.js` +1 (`attachWsMatchWorker()`'s resolved `matchId` matches the worker's own `ready` message exactly). Full suite (**2835 tests**) and typecheck green |
| **T-030** | Client `localOwner` seam — **119 owner-literal sites** (68 comparisons, 29 owner arguments, 22 property paths); **110 collapse to one seam**, 9 are the harder "the enemy is `ai`" assertion and must be redesigned or scoped out. ⚠️ **Never `sed` this**: `data.js:124` defines a commodity whose id is literally `"ai"` — the rename must be site-by-site | ADR-0008 | T-012 | ⚪ | Client renders correctly as **either** seat; golden HUD/render tests updated; the 9 hard sites individually resolved |
| **T-031** | Seat identity: display names, per-seat colours beyond the hardcoded two | FR-12 | T-030 | ⚪ | No `"player"`/`"ai"` string reaches the UI |
| **T-032** | Latency handling: local prediction of selection and camera, server-confirmed orders | FR-11 | T-013, T-026 | ⚪ | Playable at 150 ms simulated RTT; measured, not asserted |

---

## Phase 4 — Multiplayer that is pleasant to use
**Milestone M4: lobby, AI fill, disconnect survival, spectating.**

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-033** | Lobby model: create / list / join, seat kinds, seat tokens | FR-1, FR-2 | T-029 | ⚪ | Lobby survives a server restart via `/data` |
| **T-034** | Lobby UI, and a shareable join link | FR-1, FR-2 | T-033 | ⚪ | A stranger joins from a link in under 60 s |
| **T-035** | Match lifecycle: start conditions, **AI fill for open seats**, end and score screen | FR-3, FR-4, FR-6 | T-033 | ⚪ | A solo arrival gets an immediately playable match against AI |
| **T-036** | Disconnect → AI takeover → reclaim with the seat token | FR-5 | T-035 | ⚪ | Close the tab mid-match, rejoin, resume the same seat; the match never stops for others |
| **T-037** | Spectator seats (full-map vision, read-only) — **repurpose Observer Mode** (`observer.js`, `observerPanel.js`), which is already exactly this client | FR-7 | T-028 | ⚪ | A spectator cannot issue any command; host can disable spectators |
| **T-038** | In-match text chat | FR-12 | T-026 | ⚪ | Chat is rate-limited and length-capped |
| **T-039** | Rate limiting and abuse guards on every client-driven path | FR-10 | T-021 | ⚪ | A flooding client is throttled, then disconnected, without affecting the match |
| **T-040** | Desync detection via state fingerprint reporting | FR-20 | T-024 | ⚪ | An artificially divergent client is detected and logged |

---

## Phase 5 — N seats
**Milestone M5: a 4-seat free-for-all plays to a winner.**

Bumps `SAVE_VERSION` to 2 and re-baselines determinism fixtures for N ≥ 3 (ADR-0008) — **planned,
announced, and confined to this phase**.

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-041** | `ownerDefs` built from lobby config; N-seat `state.owners` | FR-1 | T-035 | ⚪ | 4-seat AI-only match plays headlessly to a winner |
| **T-042** | `state.controllers{}` replacing the 2-slot `state.ai`/`state.playerAi` | ADR-0008 | T-041 | ⚪ | N AI seats each act on their own budget; `test/ownerScaffold.test.js` extended |
| **T-043** | `opponentsOf()` replacing `otherOwner()`'s "exactly one enemy" axiom | ADR-0008 | T-042 | ⚪ | AI targets sensibly with 3+ opponents |
| **T-044** | Radial map generator for N ≥ 3 start positions; fairness checked | FR-1 | T-041 | ⚪ | Start positions equidistant and resource-fair; **2-seat path byte-identical** |
| **T-045** | N-fog / N-controller save shape; `SAVE_VERSION` → 2 | FR-19 | T-042 | ⚪ | Round-trip test at N=4; version gate rejects v1 saves cleanly |
| **T-046** | Elimination events, surrender, and last-seat-standing victory at N | FR-6 | T-041 | ⚪ | Eliminated player becomes a spectator; match continues |
| **T-047** | Re-baseline determinism fixtures for N ≥ 3; **2-seat replays preserved** | NFR-8 | T-044 | ⚪ | `test/determinism*.test.js` green; 2-seat fixtures unchanged |
| **T-048** | Sweep the remaining skirmish-critical owner literals (~18 engine sites) | ADR-0008 | T-043 | ⚪ | No skirmish-path engine line compares an owner to a literal |

---

## Phase 6 — Agents play
**Milestone M6: a Claude agent and a human play a full match to a decided result.**

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-049** | MCP transport: Streamable HTTP + JSON-RPC 2.0 core, zero dependencies, targeting revision **`2026-07-28`** — which **removed** `initialize`/`initialized`, `Mcp-Session-Id`, the GET SSE endpoint and `Last-Event-ID` resumability | FR-13, NFR-5 | T-027 | ⚪ | `tools/list`/`tools/call` verified against golden transcripts and a real MCP client. **Re-verify the spec revision before coding** — no pre-2026 tutorial or SDK example shows this shape |
| **T-049a** | Seat handle as an explicit tool argument (the spec's "Stateful Tools" pattern), since protocol-level sessions no longer exist | FR-13 | T-049 | ⚪ | A seat handle minted by `join_match` authorizes every later call |
| **T-050** | Seat auth: per-seat tokens; an agent can act **only** on its own seat | FR-18 | T-049 | ⚪ | Adversarial test: cross-seat command rejected |
| **T-051** | Lobby tools — `list_matches`, `join_match`, `leave_match` | FR-13 | T-049, T-033 | ⚪ | An agent joins a match unaided |
| **T-052** | Observation tools — `get_situation`, `list_entities`, `get_map_overview`, `get_tech_options`, **fog-respecting and summarized** | FR-14 | T-028, T-049 | ⚪ | Digest fits a reasonable context; test proves nothing outside the seat's fog leaks |
| **T-053** | Action tools — batched, group-oriented, mapped onto the Phase 2 codec | FR-15 | T-052, T-022 | ⚪ | An agent commands 20 units in one call; validation identical to a human's |
| **T-054** | `wait_for_event` — **bounded well under the client tool-call timeout**, returning "nothing yet" on expiry rather than erroring | FR-17 | T-052 | ⚪ | A repeated wait loop is cheap, bounded and safe |
| **T-055** | MCP resources — unit stats, counter triangle, build costs, tech tree | FR-16 | T-049 | ⚪ | An agent reads the rules once instead of re-deriving them |
| **T-056** | Agent APM budget, reusing the existing `aiApm` mechanism | §6.3, ADR-0007 | T-053 | ⚪ | Agent actions/minute are capped and the cap is visible to opponents |
| **T-057** | Turn-gated evaluation mode for benchmarking (never in a lobby with a human) | §6.3 | T-056 | ⚪ | A seeded match with a scripted agent replays to a known outcome |
| **T-058** | Reference scripted agent + agent developer guide | P2 | T-055 | ⚪ | A third party connects an agent using the docs alone |

---

## Phase 7 — Harden and launch
**Milestone M7: v1.**

| ID | Task | Serves | Depends | Status | Exit criteria |
|---|---|---|---|---|---|
| **T-059** | Lobby and match-result persistence to `/data` (live match state already covered by T-029a) | FR-22 | T-033, T-008a | ⚪ | A Space restart loses neither the lobby nor recorded results |
| **T-060** | Sleep/wake UX: friendly loading state and WebSocket retry with backoff for the `503` a sleeping Space returns. **No keep-alive pinger** — Spaces have been paused for abuse over exactly that | Risk, ADR-0010 | T-059 | ⚪ | First visitor after 48 h idle sees a loading state, not an error |
| **T-061** | Structured logging and operational metrics | Ops | T-029 | ⚪ | Desyncs, disconnects, match durations, tick overruns all observable |
| **T-062** | Load test: concurrent matches to the measured ceiling | NFR-4 | T-014 | ⚪ | Ceiling documented; graceful degradation verified, not assumed |
| **T-063** | Security review: transport, codec, auth, rate limits | FR-10 | T-039, T-050 | ⚪ | `/security-review` clean; adversarial suite green |
| **T-064** | Player-facing docs and the in-game help overlay updated for multiplayer | P1 | T-035 | ⚪ | A new player understands seats, AI fill and reconnect without asking |
| **T-065** | Launch checklist: PRD §9 success criteria all demonstrated | §9 | all | ⚪ | All six criteria met and evidenced |

---

## Unverified assumptions being carried

The platform dossier flags **13** items it could not verify, rather than guessing at them. The five
that would actually hurt, each owned by a task:

| # | Assumption | Owned by |
|---|---|---|
| ~~U3~~ | ~~Whether `/data` persists with no bucket attached~~ — **RESOLVED by T-008a: it does not.** Measured across two real deploys, ~2 minutes apart: the second found no trace of the first's marker. See `docs/analysis/04-hf-deployment.md` §14 | ✅ T-008a → **T-008b** |
| ~~U7/U11~~ | ~~The git push itself, and whether overwriting a Docker Space escapes the paid-plan rule~~ — **RESOLVED by T-007a.** Push works on a free account; the Space rebuilt in 41 s and served anonymously | ✅ T-007a |
| U5 | Storage-bucket write latency for frequent small writes — it is object storage, not POSIX. Benchmark before choosing a snapshot interval | T-029a |
| U12 | 2-vCPU headroom for N-player 20 Hz. The measured WebSocket result was one client against a trivial app | T-014, T-062 |
| U2 | Cold-start from a real 48 h sleep — still undocumented. (T-007a measured a *rebuild* at 41 s, which is a different path: no VM re-provisioning.) Time a cold visit after 48 h idle | T-060 |

---

## Blocked / needs a decision

| # | Question | Blocks | Owner |
|---|---|---|---|
| **Q1** | Make the Space **public**? **Confirmed hard blocker** — a private Space returns `404` to everyone but the owner and collaborators, for the running app as well as the source. G1 is unreachable while it stays private. | T-008, and all of Phases 3–7 in production | alma92350 |
| **Q2** | ~~Free tier or paid always-on?~~ **Answered by ADR-0010: start free.** 48 h idle tolerance is ample; `$0.03/h` CPU Upgrade removes sleep later if the game gets traction. Open only as a **risk**: whether a non-PRO account can rebuild an existing Docker Space — resolved by T-007a. | T-007a | — |
| **Q3** | Is multiplayer **Odyssey** a wanted v2? Shapes how much generality Phase 5 builds. | T-041 scope | alma92350 |
| **Q4** | Are agent seats visibly labelled to human opponents? (Recommendation: **yes**.) | T-031 | alma92350 |
| **T-008b** | Attach a Storage Bucket at `/data` — confirmed **free**, not gated behind PRO. `hf buckets create SpaceCities-state`, then attach read-write from Space settings. This session has no `hf` CLI or write-scoped HF token to do it directly | ADR-0012, all of Phase 3's match-persistence work | alma92350, or a future session with `hf` CLI write access |

---

## Deferred (explicitly not v1)

| Item | Why | Revisit |
|---|---|---|
| Multiplayer Odyssey / galaxy | Persistent multi-world sandbox; a product of its own | v2 |
| Ranked ladder / Elo | Single-player Elo exists; server-side identity is a separate product | v2 |
| Teams / alliances | `teamOf()` must thread through combat, auras, fog sharing, victory (ADR-0008 Phase 4) | v2 |
| User accounts | Login friction opposes G1 | v2 |
| Replay playback UX | Recording ships in T-024; playback UI does not | v2 |
| Multiplayer scenarios | `engine/scenarios.js` is single-player scripted content | — |
